export function SubmitModal({ quote, painting, onSubmitPainting }) {
  return (
    <div>
      <h1>Submit Modal</h1>
      <button onClick={() => console.log("Confirm clicked")}>Confirm</button>
    </div>
  );
}
